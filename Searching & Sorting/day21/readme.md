# day 21

=> insertion sort
similiar to playing cards
Start from the second element (index 1), since the first element is “already sorted”.
Compare it with the previous elements.
Move all elements greater than the current element one position ahead.
Insert the current element in its correct position.
Repeat until the entire array is sorted.

# Time complexity: O(n)
# space complexity: O(1)