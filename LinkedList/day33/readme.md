# day 33
Leetcode problem no: 203, Remove Linked  List elements

use 2 pointer approach, keep track of previous element and the current element
Also known as Sentinel node(previous node): consistently watch the next element => to remove the nodes at the head